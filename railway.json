{
  "$schema": "https://railway.app/railway.schema.json",
  "build": {
    "builder": "NIXPACKS"
  },
  "deploy": {
    "startCommand": "cd facebook_django && python manage.py migrate && python manage.py collectstatic --noinput && gunicorn facebook_django.wsgi:application --bind 0.0.0.0:$PORT",
    "healthcheckPath": "/admin/login/",
    "restartPolicyType": "ON_FAILURE",
    "restartPolicyMaxRetries": 10
  }
}
